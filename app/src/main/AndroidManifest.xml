<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.halbert.iserve">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IServe">
        <activity android:name=".CartaListActivity"></activity>
        <activity android:name=".CrearCartaActivity" />
        <activity android:name=".CartaActivity" />
        <activity android:name=".ShowPlatActivity" />
        <activity android:name=".CrearBegudaActivity" />
        <activity android:name=".BegudaActivity" />
        <activity android:name=".CrearReceptaActivity" />
        <activity android:name=".PlatActivity" />
        <activity android:name=".CrearProducteActivity" />
        <activity android:name=".ProducteActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".AuthActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>